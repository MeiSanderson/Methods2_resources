knitr::opts_chunk$set(echo = TRUE)
library(pacman)
pacman::p_load(rstanarm)
b1 = 23
b0 = 10
n = 1000
error = rnorm(n)
x = seq(0, n, length.out = 1)
x = rnorm(n)
y = b0 + b1 * x + error
b1 = 23
b0 = 10
n = 1000
error = rnorm(n)
x = seq(0, n, length.out = 1)
x = rnorm(n)
y = b0 + b1 * x + error
myLinearReg = data.frame(x = x, y = y)
plot(myLinearReg)
formula = b0 + b1 * x + error # from before, exp. 2
stand_glm(formula, prior = default_prior_coef(family))
library(pacman)
pacman::p_load(rstanarm)
formula = b0 + b1 * x + error # from before, exp. 2
stand_glm(formula, prior = default_prior_coef(family))
formula = b0 + b1 * x + error # from before, exp. 2
stan_glm(formula, prior = default_prior_coef(family))
stan_glm(formula)
formula = y ~ b0 + b1 * x + error # from before, exp. 2
stan_glm(formula, prior = default_prior_coef(family))
stan_glm(y ~ b0 + b1 * x + error, prior = default_prior_coef(family))
formula
error
formula = y ~ b0 + b1 * x + error # from before, exp. 2
stan_glm(y ~ x, data = myLinearReg , prior = default_prior_coef(family))
plot(stan_glm(y ~ x, data = myLinearReg , prior = default_prior_coef(family)))
